


- error test script
I get the following error:
mduby (main) CodexGeneSetExtractorDemo $ ./scripts/test-mcp.sh 
[test-mcp] GET https://translator.broadinstitute.org/genetics_provider/mcp_geneset
[test-mcp] HTTP 401
[test-mcp] POST tools/list
<!doctype html>
<html lang=en>
<title>404 Not Found</title>
<h1>Not Found</h1>
<p>The requested URL was not found on the server. If you entered the URL manually please check your spelling and try again.</p>

the tool list POST address that works in postman is https://translator.broadinstitute.org/genetics_provider/mcp_geneset/mcp?stream=1


- reaffirm mcp server
the mcp server address is https://translator.broadinstitute.org/genetics_provider/mcp_geneset, although it might change later


- create repo
I am creating a directory/repo for a demo session using a gene set mcp server to display our data
the users will check out this repo to use as a codex plugin
make this demo directory/repository self-contained and give them either a one-command bootstrap or a prebuilt dev
the mc server address is https://translator.broadinstitute.org/genetics_provider/mcp_geneset
Put these in the demo directory:

  README.md
  AGENTS.md
  .codex/
    config.toml.example
  .agents/
    skills/
      gene-set-provenance/
        SKILL.md
  .devcontainer/
    devcontainer.json
    Dockerfile
  scripts/
    setup-demo.sh
    test-mcp.sh
  examples/
    prompts.md
    expected-results.md

the flow for the user will be:
git clone https://github.com/YOUR-ORG/gene-set-codex-demo.git
cd gene-set-codex-demo
./setup-demo.sh
codex

add in a bootstrap script
the mcp server is hosted remotely at translator.broadinstitute.org/gete-set-mcp/ and has the following properties:
Streamable HTTP MCP server
read-only tools
bearer token auth with value 'change-me'
rate limits

I also want this demo to be runnable in github codespaces

